{
  "name": "bolao-copa-2026",
  "version": "0.0.0",
  "private": true,
  "description": "Bolão Copa 2026 — app estático (index.html). Este package.json existe só para scripts utilitários (backup); o app não tem build.",
  "type": "module",
  "scripts": {
    "backup": "node scripts/backup-supabase.mjs",
    "seed:teste": "node scripts/mata-teste.mjs seed",
    "clean:teste": "node scripts/mata-teste.mjs clean",
    "seed:chave": "node scripts/seed-bracket.mjs seed",
    "clean:chave": "node scripts/seed-bracket.mjs clean",
    "seed:16avos": "node scripts/seed-16avos.mjs",
    "dev": "npx serve -p 3333 ."
  }
}
